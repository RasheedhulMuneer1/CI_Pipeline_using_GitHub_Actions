name: Node.js CI

on: 
  push:
    branches: [ "master" ]

jobs: 
  build:
    runs-on: ubuntu-latest


    steps: 
    - uses: actions/checkout@v3

    -name: Set up Node.js
     uses: actions/setuo-node@v3
     with:
       node-version: '20'

    -run: npm install
    -run: npm test       